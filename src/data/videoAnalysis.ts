import type { VideoAnalysis } from '../types/dashboard';

// Intentionally empty: dashboard video analysis comes from the real social LIVE snapshot/API.
export const videoAnalysis: VideoAnalysis[] = [];
