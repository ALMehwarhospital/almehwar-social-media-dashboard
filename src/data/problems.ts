import type { DetectedProblem } from '../types/dashboard';

// No separate problem register has been approved yet. Issues stay inside Recommendations until promoted.
export const problems: DetectedProblem[] = [];
