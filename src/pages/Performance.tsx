import { useState } from 'react';import { useFilters } from '../utils/FilterContext';import { getMonthlyPerformance } from '../utils/selectors';import { SectionHeader,Card,EmptyState } from '../components/dashboard/Primitives';import { MonthlyTrendChart } from '../components/charts/MonthlyTrendChart';import { FunnelView } from '../components/charts/FunnelView';import { formatNumber } from '../utils/format';
export default function Performance(){const{month}=useFilters();const[scope,setScope]=useState<'total'|'organic'|'paid'>('total');const current=getMonthlyPerformance(month);if(!current)return <EmptyState message='No data for the selected month.'/>;return <div className='space-y-10'><SectionHeader eyebrow='Trends' title='Performance Over Time' description='Compare months across every core metric, split by organic, paid, or total.' action={<div className='flex bg-warm-100 rounded-full p-1'>{(['total','organic','paid'] as const).map(s=><button key={s} onClick={()=>setScope(s)} className={`text-xs font-semibold px-3.5 py-1.5 rounded-full capitalize ${scope===s?'bg-navy-900 text-warm-50':'text-fog-500'}`}>{s}</button>)}</div>}/><Card><MonthlyTrendChart scope={scope}/></Card><section className='grid grid-cols-1 lg:grid-cols-2 gap-6'><Card><SectionHeader eyebrow='Conversion' title='Marketing Funnel'/><FunnelView funnel={{reach:current.total.reach,profileVisits:current.total.profileVisits,linkClicks:current.total.linkClicks,leads:current.total.leads,profileVisitRate:current.total.reach?current.total.profileVisits/current.total.reach*100:0,clickRate:current.total.profileVisits?current.total.linkClicks/current.total.profileVisits*100:0,leadRate:current.total.linkClicks?current.total.leads/current.total.linkClicks*100:0}}/></Card><Card><SectionHeader eyebrow='Attribution' title='Paid vs Organic'/><div className='space-y-3'>{(['reach','interactions','linkClicks','leads'] as const).map(key=>{const organic=current.organic[key],paid=current.paid[key],total=organic+paid||1;return <div key={key}><div className='flex justify-between text-xs text-fog-500 mb-1 capitalize'><span>{key==='linkClicks'?'Link Clicks':key}</span><span>{formatNumber(total)}</span></div><div className='h-6 rounded-full overflow-hidden flex bg-warm-100'><div className='bg-mint-500' style={{width:`${organic/total*100}%`}}/><div className='bg-signal-amber' style={{width:`${paid/total*100}%`}}/></div></div>})}</div></Card></section></div>}