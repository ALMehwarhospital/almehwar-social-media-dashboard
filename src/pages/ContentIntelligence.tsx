import { useMemo,useState } from 'react';import { useFilters } from '../utils/FilterContext';import { getContent,pillarSummary,formatSummary } from '../utils/selectors';import { SectionHeader,Card,EmptyState } from '../components/dashboard/Primitives';import { ContentTable } from '../components/content/ContentTable';import type { ContentItem } from '../types/dashboard';import { formatNumber,formatPercent } from '../utils/format';
const RANK=[['reach','Reach'],['engagementRate','Engagement Rate'],['interactions','Interactions'],['followersGained','Followers Gained'],['linkClicks','Link Clicks']] as const;
export default function ContentIntelligence(){const{month,platform,pillar,format,spendType}=useFilters();const[rank,setRank]=useState<keyof ContentItem>('reach');const items=useMemo(()=>getContent(month,{platform:platform==='All'?undefined:platform,pillar:pillar==='All'?undefined:pillar,format:format==='All'?undefined:format,spendType:spendType==='All'?undefined:spendType}),[month,platform,pillar,format,spendType]);if(!items.length)return <EmptyState message='No content for this selection.'/>;const sorted=[...items].sort((a,b)=>Number(b[rank])-Number(a[rank]));const pillars=pillarSummary(month).sort((a,b)=>b.avgEngagement-a.avgEngagement);const formats=formatSummary(month).sort((a,b)=>b.avgEngagement-a.avgEngagement);return <div className='space-y-10'><SectionHeader eyebrow='Content' title='Content Intelligence' description='Rank content by the outcome that matters — not views alone.' action={<select value={String(rank)} onChange={e=>setRank(e.target.value as keyof ContentItem)} className='text-xs bg-white border rounded-full px-3 py-2'>{RANK.map(([k,l])=><option key={k} value={k}>{l}</option>)}</select>}/><section className='grid grid-cols-1 lg:grid-cols-2 gap-6'><Card><p className='text-xs uppercase tracking-wide text-mint-600 font-semibold mb-3'>Top Performing Content</p><ContentTable items={sorted.slice(0,5)} metric={rank}/></Card><Card><p className='text-xs uppercase tracking-wide text-signal-coral font-semibold mb-3'>Underperforming Content</p><ContentTable items={sorted.slice(-5).reverse()} metric={rank}/></Card></section><section><SectionHeader eyebrow='Pillars' title='Content Pillar Analysis'/><div className='grid grid-cols-1 md:grid-cols-3 gap-4'>{pillars.slice(0,6).map(p=><Card key={p.pillar}><p className='font-display text-lg'>{p.pillar}</p><p className='text-xs text-fog-500 mt-1'>{p.posts} posts</p><div className='mt-4 text-sm space-y-1'><p>Avg Reach <b>{formatNumber(p.avgReach)}</b></p><p>Avg Engagement <b>{formatPercent(p.avgEngagement)}</b></p><p>Followers <b>+{p.followersGained}</b></p></div></Card>)}</div></section><section><SectionHeader eyebrow='Formats' title='Format Analysis'/><div className='grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3'>{formats.map(f=><Card key={f.format}><p className='font-medium'>{f.format}</p><p className='font-display text-2xl mt-2'>{formatPercent(f.avgEngagement)}</p><p className='text-xs text-fog-500'>Avg engagement</p></Card>)}</div></section></div>}